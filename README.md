experiment react router, user permissions, login, signUp
===============

~~~

### Install 3-d party dependencies and running app

**NPM**

~~~
npm install
~~~
Then run project:
~~~
npm start

npm run build
```

The app will be available on host:
```
localhost: 3000
```